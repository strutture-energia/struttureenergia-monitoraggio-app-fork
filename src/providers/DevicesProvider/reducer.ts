import { DevicesProviderState } from "./DevicesProvider";

export function devicesReducer(
  state: DevicesProviderState, 
  action: string
): DevicesProviderState {
  switch(action) {
    default:
      return state;
  }
}